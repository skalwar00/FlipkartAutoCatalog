Analyze the entire project carefully before making changes.

Do NOT randomly move files.

First identify:
- frontend files
- backend files
- Python automation scripts
- shared libraries
- config files
- build system
- API routes
- upload/output folders

Then execute this migration step-by-step safely.

========================================
STEP 1 — ANALYZE PROJECT
========================================

Create a clear report of:

1. Frontend location
2. Backend location
3. Python scripts location
4. Shared libraries location
5. Current import aliases
6. Vite config
7. Backend entry point
8. Python execution flow
9. Static assets
10. Environment variables
11. Build commands

Do NOT modify anything yet.

========================================
STEP 2 — IDENTIFY ARCHITECTURE
========================================

Identify and label:

- React/Vite frontend
- Node/Express backend
- Python processing modules
- Shared code modules
- API client libraries

Explain how they currently communicate.

========================================
STEP 3 — MOVE FRONTEND
========================================

Create:

/frontend

Move ONLY frontend-related files into /frontend.

After moving:
- fix all broken imports
- fix tsconfig paths
- fix Vite aliases
- fix asset paths
- ensure package.json works
- ensure frontend can run independently

Do NOT touch backend yet.

========================================
STEP 4 — TEST FRONTEND BUILD
========================================

Run and verify:

pnpm install
pnpm build

Fix:
- import errors
- alias errors
- missing assets
- TypeScript errors
- Vite config issues

Do not continue until build succeeds.

========================================
STEP 5 — MOVE BACKEND
========================================

Create:

/backend

Move ONLY backend-related files into /backend.

After moving:
- fix imports
- fix server entry path
- fix routes
- fix static file paths
- fix upload/output directories
- ensure backend runs independently

========================================
STEP 6 — MOVE PYTHON SCRIPTS
========================================

Create:

/python

Move:
- XLS scripts
- image processing scripts
- utility Python scripts

Then:
- fix all execution paths
- fix subprocess exec paths
- ensure backend can execute Python scripts correctly

Example:
exec("python ../python/fill_xls.py")

========================================
STEP 7 — SHARED LIBRARIES
========================================

Create:

/shared

Move reusable libraries:
- api-zod
- api-spec
- api-client-react
- shared types
- shared utilities

Fix all references/imports.

========================================
STEP 8 — FINAL VALIDATION
========================================

Verify:

- frontend builds successfully
- backend starts successfully
- Python scripts execute successfully
- uploads work
- output generation works
- XLS automation still works
- image processing still works
- API communication still works

========================================
STEP 9 — DEPLOYMENT READY
========================================

Prepare:
- frontend for Vercel
- backend for Railway

Generate:
- clean package.json files
- .env examples
- deployment instructions

========================================
IMPORTANT RULES
========================================

- Preserve all functionality
- Do NOT rewrite working logic unnecessarily
- Do NOT remove business logic
- Do NOT simplify working code
- Keep all APIs compatible
- Keep existing XLS automation intact
- Keep existing image processing intact
- Avoid introducing breaking changes
- Make changes incrementally and safely
- After each step, validate before continuing